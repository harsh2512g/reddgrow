{
  "name": "@threadsignal/tracking",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json && node build-snippet.mjs",
    "dev": "tsc -p tsconfig.build.json --watch --preserveWatchOutput",
    "lint": "eslint src tests build-snippet.mjs",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "4.6.4"
  },
  "devDependencies": {
    "esbuild": "0.28.2",
    "@types/node": "24.13.4"
  }
}
