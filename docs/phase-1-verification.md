# Phase 1 verification record

Date: 2026-09-15. Scope: authentication, organizations, membership roles, RLS, trials, central plan configuration, and settings. The repository had completed Phase 0 functional checks when the owner requested the next unfinished phase. Phase 2 has not started. No commit, push, external login, deployment, existing dotenv read, or external credential use occurred.

Every package command below uses `./scripts/local` as its prefix. It clears inherited environment variables, uses public npm, verifies dedicated Colima ownership, and keeps configurable state under the repository. Local Auth uses only synthetic users and the project Supabase/Mailpit services. The historical Phase 0 Lima temporary-path exception remains recorded separately; it is not erased by this work.

## Command evidence

| Command after `./scripts/local`                          | Result                                                                                                                                                         |
| -------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `pnpm install --no-frozen-lockfile`                      | PASS, exit 0; installed Supabase SSR/client, React Hook Form/resolvers, Lucide, workspace dependencies; second install aligned React/React DOM 19.3.0          |
| `pnpm install --frozen-lockfile`                         | PASS, exit 0; all 15 workspace projects, pnpm 12.4.1                                                                                                           |
| `pnpm lint`                                              | PASS, exit 0                                                                                                                                                   |
| `pnpm typecheck`                                         | PASS, exit 0; 19 Turbo tasks plus tooling TypeScript                                                                                                           |
| `pnpm test`                                              | PASS, exit 0; 194 tests across 30 files, final run 6.42 seconds                                                                                                |
| `pnpm build`                                             | PASS, exit 0; 14 successful Turbo tasks (13 cached), final build 9.564 seconds                                                                                 |
| `pnpm extension:build`                                   | PASS, exit 0; extension remains a manual-only shell with no Reddit submission capability                                                                       |
| `pnpm db:reset`                                          | PASS, exit 0; applied Phase 1 migration and synthetic seed                                                                                                     |
| `pnpm seed`                                              | PASS, exit 0; idempotent rerun preserves existing local records                                                                                                |
| `pnpm db:types`                                          | PASS, exit 0; regenerated from migrated local PostgreSQL, including optional billing-contact RPC argument                                                      |
| `pnpm db:lint`                                           | PASS, exit 0; foundation assertions and Supabase SQL lint                                                                                                      |
| `pnpm test:integration`                                  | PASS, exit 0; 21 tests across 2 files, final rerun 4.45 seconds, including 17 Phase 1 security/concurrency cases and 4 service/provider/worker cases           |
| `pnpm test:e2e`                                          | PASS, exit 0; 15 passed, 1 intentional skip; final full rerun 22.3 seconds, including restored four-member fixture                                             |
| `pnpm test:e2e shell.spec.ts`                            | PASS, exit 0; all 10 public/service-off browser tests passed, 4.7 seconds, with Colima stopped                                                                 |
| `pnpm services:start`                                    | PASS, exit 0; project Colima, Supabase, Redis                                                                                                                  |
| `pnpm services:health`                                   | PASS, exit 0; exact Colima context/project socket, Redis PONG, Supabase running                                                                                |
| `pnpm services:stop`                                     | PASS, exit 0; final service shutdown verified before service-off browser tests; local data retained                                                            |
| `pnpm format:check`                                      | PASS, exit 0; all matched files use Prettier style                                                                                                             |
| `pnpm format`                                            | PASS, exit 0; source and documentation formatted                                                                                                               |
| `pnpm secrets:check`                                     | PASS, exit 0; 254 repository text files at the final hygiene check; no tracked env/dependencies, supported secret patterns, or operational employer references |
| `pnpm dev`                                               | PASS startup and live HTTP checks; 11 application/package watchers, Next ready, worker started; left running intentionally                                     |
| Local Node HTTP probe                                    | PASS, exit 0; homepage, web readiness, and worker readiness each returned HTTP 200 with expected ready state                                                   |
| `git diff --check` (repository-only, no launcher needed) | PASS, exit 0                                                                                                                                                   |

Phase 1 local acceptance is verified and ready for review. All failures listed below were corrected or have explicit external-verification limits. Project services and the web/worker development processes are intentionally left running after the final restart.

The one intentional E2E skip is the duplicate mobile seeded-role matrix. The desktop matrix explicitly resizes to mobile and checks drawer navigation/overflow; fresh onboarding and logout/relogin run in both desktop and mobile projects. No failed test is converted to a skip. Auth tests require the verified local service marker and are explicitly skipped in the hosted static CI job; the Colima CI job runs the full suite.

## Acceptance evidence

| Requirement                              | Evidence                                                                                                                                                                                                                                                |
| ---------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Login, refresh, protected routes, logout | Real local Supabase email-link exchange, HTTP-only cookie assertions, logout cleanup, protected destination restoration; 50 focused auth/access unit tests cover refresh, flow selection, safe redirects, origin checks, native Google form, and errors |
| Google OAuth contract                    | Guarded configured-HTTPS code and unit tests; intentionally disabled locally and not externally live-tested                                                                                                                                             |
| Organization onboarding                  | Browser creates a fresh organization; SQL atomically creates verified owner, seven-day trial, subscription and audit event                                                                                                                              |
| Membership roles                         | Owner/admin/member/viewer SQL tests plus real-session API/UI role matrix; direct member/viewer PATCH denied and nonowner billing access denied                                                                                                          |
| Organization isolation                   | SQL RLS denies cross-tenant tables/RPCs; real-session API returns 404 for another organization; forged selection cookie cannot switch to an unauthorized workspace                                                                                      |
| Invitation by email                      | Local console-mode invitation, private one-time copy link, verified matching-email acceptance, replay/expiry tests, seat reservation and concurrent capacity checks                                                                                     |
| Trial and central plans                  | Config/catalog parity tests; one-trial race protection; expired trial and downgrade tests retain data while denying new allocation                                                                                                                      |
| Settings shell                           | Organization, team, plan/usage and integration screens use actual local records; export/deletion requests are recorded, not processed                                                                                                                   |
| Distinctive accessible UI                | SVG/CSS signal artwork, editorial/system fonts, responsive nav and forms; fresh public desktop/mobile screenshots reviewed; authenticated screenshots require loaded dashboard                                                                          |
| Isolation and phase scope                | Fixed mock/console/fixture modes; generated public anon key only in web runtime; no Phase 2 business tables; no commit/push; dependencies/local state ignored                                                                                           |

## Failed attempts and corrections

| Attempt                                    | Actual result                                                                                                                                                                                                                   | Correction                                                                                                                                                                |
| ------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Initial shared button interaction test     | React hook failure; Radix resolved React 19.2.8 while app renderer used 19.3.0                                                                                                                                                  | Workspace React/React DOM override and aligned peer range; targeted UI 10/10 passed                                                                                       |
| Initial scoped auth/UI lint                | Control-character regex/unused argument and component prop/static component errors                                                                                                                                              | Corrected source; final lint passed                                                                                                                                       |
| Early web TypeScript check                 | New tables/RPCs appeared as `never` through old compiled database declarations                                                                                                                                                  | Generated local types and built prerequisite packages; full typecheck passed                                                                                              |
| Concurrent checks during dependency update | `ERR_PNPM_LOCKFILE_CONFIG_MISMATCH`                                                                                                                                                                                             | Completed authorized install, then resumed checks; final frozen installation passed                                                                                       |
| Initial live SQL attempt                   | Sandbox denied Colima socket; suite failed before SQL and skipped its cases                                                                                                                                                     | Reran against the same owned services with local sandbox access; all integration cases passed                                                                             |
| Restricted production build                | Turbopack subprocess port binding denied                                                                                                                                                                                        | Authorized local process access; repeated compiler error persisted in generated cache, so removed only `apps/web/.next` and rebuilt successfully                          |
| Initial local inbox diagnostic             | `tsx` IPC binding denied; follow-up saw inbox unavailable during a deliberate service restart                                                                                                                                   | Used the normal isolated test runner once project services were ready; no external inbox used                                                                             |
| First full browser run                     | 12 passed, 2 failed: desktop still showed pending send after a 5-second assertion; mobile logout left a PKCE flow cookie                                                                                                        | Bounded 15-second cold sign-in wait; recognize and clear installed Supabase flow/index verifier cookies; rerun passed                                                     |
| Repeated invitation journey                | 14 passed, 1 failed after the 180-second role-test timeout, 1 intentional skip. A previous synthetic teammate remained because cleanup queried the team before streamed content loaded; the app correctly filled all five seats | Test cleanup waits for loaded team headings, removes only its own narrowly named synthetic invitees, and verifies four seats before and after the journey                 |
| Service-off public smoke                   | 8 passed, 2 failed: Next proxy pipeline rejected a relative fallback Location with Invalid URL                                                                                                                                  | Use the validated configured application origin for an absolute fallback redirect, never the incoming Host header; all 10 service-off smoke tests passed after correction |
| Initial authenticated screenshots          | Captured loading skeleton before dashboard content                                                                                                                                                                              | Wait for the actual dashboard heading and verify overflow before taking screenshots                                                                                       |

Development printed the pinned pnpm Node-fallback notice because its optional native install script is disabled; web/worker startup and all watchers succeeded. Package installation warned that the selected ESLint version is deprecated and one public registry request was slow; installation still exited successfully. A read-only review initially suggested replacing Next's `retry` error-boundary prop with `reset`; installed Next 16.3 documentation confirmed `retry` is correct, so that suggestion was withdrawn without a code change. All meaningful failures above remain visible rather than being reported as passes.

## Manual review and limitations

Run `./scripts/local pnpm dev` with project services active. Open `http://127.0.0.1:3000`, sign in as `owner@threadsignal.test`, and open the latest message in `http://127.0.0.1:54324` in the same browser. Review the dashboard, organization, team, plan, and integration pages; repeat with member/viewer identities to see restricted controls. Use a new synthetic email for onboarding and the seeded Growth workspace for invitations.

Google OAuth, real Reddit/AI/email/billing/crawler providers, deployed HTTPS behavior, remote GitHub Actions, and the declared Node 24 CI runtime were not live-tested. Local execution used installed Node 25.2.1. Terms/privacy are marked local-development drafts. Export/deletion processing and payments remain later-phase work. No production credentials are required or requested.

The earlier [Phase 0 isolation exception](phase-0-verification.md#isolation-exception-observed) remains open for owner review; no outside-repository inspection or cleanup was attempted. This does not describe that historical run as unqualified isolation compliance. Current service restarts explicitly excluded guest DNS port 53 from forwarding.

See [file inventory](phase-1-files.md), [database design and permissions](phase-1-database.md), and [architecture decisions](../DECISIONS.md).

## Final runtime and review state

The final restart verified the repository-owned `colima` context and socket, Redis PONG, and running Supabase. The subsequent isolated development command started the Next.js app and worker. A Node fetch probe checked only `http://127.0.0.1:3000/`, web `/api/health/ready`, and worker `http://127.0.0.1:3001/api/health/ready`; all returned HTTP 200 with expected content/readiness. No environment or credentials were printed. Both applications and local services remain running for review.

Final authenticated screenshots show the rendered dashboard with the restored four-person Growth fixture at desktop and mobile widths, without horizontal overflow. The images are ignored local artifacts at `.threadsignal/verification/phase1-workspace-desktop.png` and `phase1-workspace-mobile.png`.

No new approval is needed for this local implementation. Outside-repository cleanup of the historical Phase 0 exception and any future external account/runner/deployment setup require separate authorization. Phase 2 has not begun.
